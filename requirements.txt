# AgentAudit core stack — pinned to the versions verified against installed
# source (charter rule 4: never assume SDK behavior from docs alone).
# Regenerate exact pins with: pip freeze
strands-agents==1.55.0
strands-agents-evals==1.2.0
boto3==1.43.89
jinja2==3.1.6

# Dev / test
pytest>=8.0
