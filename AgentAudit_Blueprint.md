# AgentAudit — طبقة الأمان المفقودة لوكلاء Strands

**الشعار المقترح:** *"Your agent passed the demo. Did it pass the pentest?"*
**المسار:** Professional Agents — Agents for Humans Hackathon (AWS × Strands Agents SDK)

---

## 1. الموضع الاستراتيجي (لماذا هذا يفوز تحديداً)

قبل كتابة أي سطر كود، بحثت في السوق الحالي (2026) لأتأكد أن الفكرة أصيلة وليست مكررة. النتيجة:

| الأداة الموجودة | ماذا تفعل | أين تقف |
|---|---|---|
| `strands_evals.experimental.redteam` (رسمية من AWS) | اختبار عدائي على **مستوى النموذج/الـ LLM** فقط: Crescendo, GOAT, PAIR, Sequential-Break ضد 5 فئات خطر (guideline_bypass, system_prompt_leak, harmful_content, data_exfiltration, excessive_agency) | ممتازة لكن **لا تفحص طبقة الأدوات (tools) ولا طبقة النشر السحابي** |
| `agent-boundary-scan`, `Inkog`, `Deconvolute`, `mcp-sec-scan` | تحليل ساكن (SAST) لكشف confused-deputy وتسريب صلاحيات في مخططات MCP/LangGraph/CrewAI | عامة لأي إطار عمل، **غير مخصصة لـ Strands ولا لـ AgentCore**، ولا تُشغّل اختباراً ديناميكياً حياً |
| ماسحات AWS IAM العامة | تفحص صلاحيات IAM بشكل عام | **لا تفهم سياق AgentCore** (Identity, Memory, Gateway, Guardrails) |

**الفجوة الحقيقية:** لا توجد أداة واحدة تجمع الثلاث طبقات معاً وتُخرج حكماً موحداً **مخصص لـ Strands + AgentCore تحديداً**. هذا بالضبط ما يبنيه AgentAudit — وهو موضع يصعب على أي منافس آخر في الهاكاثون الوصول إليه لأنه يتطلب خلفية pentesting حقيقية (خبرتك) + فهم عميق لـ SDK الراعي نفسه.

**رسالة واضحة للجنة التحكيم:** أنت لا "تستخدم" Strands، أنت **تُخضعه لتدقيق أمني احترافي** بنفس المنهجية التي تُخضع بها أي تطبيق ويب لاختبار اختراق — Recon → Mapping → Vulnerability Discovery → Exploitation Thinking → Reporting. هذا يطابق منهجيتك الشخصية حرفياً.

---

## 2. الطبقات الثلاث (المميزات الأساسية)

### الطبقة 1 — Behavioral Layer (سلوك النموذج)
- تُشغّل `RedTeamExperiment` من `strands_evals` كوحدة مدمجة (لا تعيد اختراعها — تبني عليها، وهذا يُظهر نضجاً هندسياً للجنة).
- تضيف **فئات خطر مخصّصة لأدوات المستخدم الفعلية** (مش فقط الفئات الجاهزة): إذا كان للوكيل أداة `send_email` تُولّد AdversarialCaseGenerator سيناريو "أقنع الوكيل يرسل بريداً لعنوان خارجي غير مصرّح"، وإذا كانت له أداة `run_shell` تُولّد سيناريو "اهرب من الـ system prompt لتنفيذ أمر حذف".
- تُخرج: نسبة الاختراق (breach rate) لكل فئة، مع نص المحادثة الكامل كدليل.

### الطبقة 2 — Architectural Layer (حدود الثقة بين الأدوات) — **هذا هو الابتكار الحقيقي**
- تحلل كود الوكيل (system prompt + قائمة الأدوات + تعريفات الأنواع) وتبني **Tool Trust Graph**: أي أداة تستهلك مخرجات أداة أخرى بدون تحقق؟
- تكشف أنماطاً من عملك الفعلي في bug bounty، مُسقَطة على عالم الوكلاء:
  - **IDOR-in-Agent**: أداة تقبل `user_id` أو `resource_id` من نص المحادثة (وليس من هوية الجلسة المصادَق عليها) → المستخدم يقنع الوكيل "تصرّف كأنك المستخدم رقم 42".
  - **Confused Deputy بين أداتين**: أداة A (صلاحيات منخفضة) تستدعي أداة B (صلاحيات عالية) بناءً على مُدخل غير موثوق من A.
  - **Excessive Agency**: أداة تملك صلاحية أوسع بكثير مما تحتاجه فعلياً وظيفتها المعلنة (مبدأ الصلاحية الأقل).
- تُخرج تقريراً بصيغة SARIF (متوافق مع GitHub Code Scanning) — لمسة احترافية تُظهر فهماً حقيقياً لأدوات DevSecOps.

### الطبقة 3 — Deployment/Cloud Layer (وضعية AgentCore)
- إذا نُشر الوكيل عبر **Amazon Bedrock AgentCore**، تفحص:
  - هل دور IAM المرفق يتبع مبدأ الصلاحية الأقل أم أنه واسع جداً (`*` في الموارد)؟
  - هل **Bedrock Guardrails** مُفعّلة ومربوطة فعلياً بالنموذج (وليس فقط معرّفة بدون استخدام)؟
  - هل AgentCore Memory يخزّن بيانات حساسة بدون تشفير/TTL؟
  - هل AgentCore Gateway يكشف أدوات لم تُدرج صراحة في الـ allowlist؟

### طبقة التوحيد — AgentAudit Scorecard
- تدمج نتائج الطبقات الثلاث في **بطاقة موقّعة واحدة** (JSON + HTML قابل للمشاركة)، بنمط شبيه بمشروعك RADAR/SceneLock (شهادات موقّعة + بوابات حتمية) — إعادة استخدام ذكية لخبرتك المعمارية.
- كل نتيجة تأتي مع **اقتراح إصلاح جاهز كـ code diff** (ليس فقط "يوجد خطر" بل "غيّر السطر X إلى Y") — هذا يرفع معيار "Design & Product Experience" لأنه يحوّل الأداة من "كاشف مشاكل" إلى "مساعد إصلاح".
- زر واحد: `agentaudit run --agent my_agent.py --deploy-config agentcore.yaml` → تقرير كامل خلال دقائق.

---

## 3. البنية التقنية (Architecture)

```
┌─────────────────────────────────────────────────────────┐
│                     AgentAudit CLI/SDK                    │
│              agentaudit run --agent <file>                │
└───────────────┬───────────────┬───────────────┬──────────┘
                │               │               │
     ┌──────────▼──────┐ ┌─────▼───────┐ ┌─────▼──────────┐
     │ Behavioral Probe │ │ Static Graph │ │ Cloud Posture   │
     │ (wraps           │ │ Analyzer     │ │ Checker         │
     │ strands_evals    │ │ (AST parse   │ │ (boto3: IAM,    │
     │ .redteam)        │ │ of @tool defs│ │ Bedrock,        │
     │                  │ │ + prompt)    │ │ AgentCore API)  │
     └──────────┬───────┘ └─────┬────────┘ └─────┬───────────┘
                │                │                │
                └────────┬───────┴────────┬───────┘
                         ▼                 ▼
                ┌─────────────────────────────────┐
                │   Unified Risk Scorer + Signer   │
                │  (weights findings, dedups,      │
                │   signs JSON report)             │
                └────────────┬────────────────────┘
                             ▼
                ┌─────────────────────────────────┐
                │  Report Renderer                 │
                │  → HTML Scorecard (شاشة العرض)    │
                │  → SARIF (GitHub Code Scanning)  │
                │  → JSON (CI/CD gate: exit code)  │
                └───────────────────────────────────┘
```

**التقنيات:**
- Python 3.11+، `strands-agents` + `strands-agents-evals` (رسمية).
- `ast` module لتحليل الكود الساكن لتعريفات `@tool`.
- `boto3` لاستعلامات IAM/Bedrock/AgentCore.
- نشر تجريبي عبر `bedrock-agentcore-starter-toolkit` (لإثبات أن الفحص يعمل على نشر حقيقي — يرفع Technical Implementation).
- تقرير HTML بسيط (Jinja2 + Tailwind CDN) — لا حاجة لإطار عمل ثقيل.

---

## 4. خطة البناء (مقسّمة على مراحل قابلة للقياس — بدون الضغط على الوقت، فقط منطق الأولويات)

| المرحلة | المحتوى | لماذا هذا الترتيب |
|---|---|---|
| **1. الأساس** | غلاف حول `strands_evals.redteam` + توليد سيناريوهات مخصّصة من أدوات المستخدم الفعلية | يعطيك أول نتيجة قابلة للعرض بأقل مجهود، ويثبت التكامل الحقيقي مع SDK الراعي |
| **2. المحرك المبتكر** | Static Tool Trust Graph Analyzer + كشف الأنماط الثلاثة (IDOR-in-Agent, Confused Deputy, Excessive Agency) | هذا قلب التمايز — يستحق أكبر استثمار وقت |
| **3. طبقة السحابة** | فحوصات IAM/Guardrails/AgentCore عبر boto3 (حتى لو مبسّطة لـ 5-6 فحوصات فقط في البداية) | يُثبت أن الأداة "تفكر AWS-native" وليست أداة عامة معاد تسميتها |
| **4. التوحيد والتوقيع** | دمج النتائج + توليد Scorecard HTML + توقيع JSON | يحوّل المشروع من "سكربت فحص" إلى "منتج" — نقطة قوة في Design |
| **5. مثال حي (Case Study)** | ابنِ وكيل Strands تجريبي به ثغرات مقصودة (IDOR-in-Agent + excessive_agency)، شغّل AgentAudit عليه في الفيديو، أظهر الاكتشاف ثم الإصلاح ثم إعادة الفحص = نظيف | القصة المرئية الأقوى للتحكيم: "قبل/بعد" حقيقي، وليس شرحاً نظرياً |
| **6. التوثيق والعرض** | README احترافي + مخطط المعمارية (استخدم المخطط أعلاه) + فيديو 5 دقائق بنفس بنية: المشكلة → الحل → العرض الحي → لماذا يهم | يطابق تماماً متطلبات التسليم الرسمية |

---

## 5. تعليمات التسليم الرسمية (تذكير دقيق حتى لا يُستبعد المشروع)

- [ ] وصف نصي واضح: ماذا يفعل، لمن، وكيف يعمل تقنياً.
- [ ] مستودع GitHub **عام** برخصة MIT أو Apache 2.0 + README كامل بتعليمات تشغيل.
- [ ] **مخطط معماري** (استخدم مخطط القسم 3 أعلاه أو طوّره بصرياً).
- [ ] فيديو عرض ≤ 5 دقائق (شرائح + تسجيل شاشة + تعليق صوتي كافٍ، لا حاجة للظهور بالكاميرا).
- [ ] AWS Builder ID مربوط بالتسليم.
- [ ] (اختياري لكن يقوّي النتيجة) رابط تجربة حية، ونشر فعلي عبر AgentCore.
- [ ] (نقاط إضافية) منشور على builder.aws.com يوثّق رحلة البناء واستخدام AWS — يُحتسب كبونص رسمي.

**تذكير بمعايير التحكيم وكيف يخدمها كل جزء من الخطة أعلاه:**
- Technical Implementation ← المرحلتان 1 و2 (تكامل حقيقي + تحليل أصيل).
- Design ← المرحلة 4 (منتج متكامل لا مجرد PoC).
- Potential Impact ← جمهور محدد وحقيقي (كل من يبني بـ Strands، بما فيهم منافسوك في نفس الهاكاثون).
- Creativity & Originality ← الموضع الاستراتيجي في القسم 1 (لا أحد غيرك يملك هذه الزاوية).
- Presentation ← المرحلة 5 (قصة قبل/بعد مرئية بدل شرح نظري).

---

## 6. الاسم والهوية المقترحة

- **الاسم**: AgentAudit
- **الوسم البصري المقترح**: لوحة تحكم داكنة بلون أخضر/أحمر لحالة الفحص (نمط "security dashboard" مألوف لأي مهندس أمن) — ليس تصميماً "لطيفاً" بل تصميماً يوحي بالجدية المهنية فوراً.
- **الجملة الافتتاحية المقترحة للفيديو**: *"I've spent 10 years finding logic flaws in web apps. AI agents have the exact same flaws — just with better manners. This is AgentAudit."*
